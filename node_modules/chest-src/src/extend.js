function extend(cls, ext) {
  var fin = cls,
      extpro = ext.prototype,
      clspro = cls.prototype;
      finpro = Object.create(extpro);

  for (var n in clspro)
    finpro[n] = clspro[n];

  finpro.__super = ext;
  finpro.__superp = extpro;

  fin.prototype = finpro;
}

m.extend = extend;
